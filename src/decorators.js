
const decorators = [
    {
        input: ["memberships"]
        ,output: function (memberships) {
            if(!memberships) {
                return;
            }

            // memberships.find(function() {
            // });
        }
    }
];

export default decorators;